/* App Config */
import { criaDiv, criaP, fechar } from './utils.js';
import { corpo } from './utils.js';

// count settings
export function configCount(elementoPai, array, tecla, fecha){
    let janela = criaDiv()
    janela.classList.add('painel')
    elementoPai.appendChild(janela)

    for(let i = 0; i < array.length; i++){

    }
}