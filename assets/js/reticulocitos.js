//Mostra o botão
let reticulocitos = document.createElement('div');
 reticulocitos.innerText = 'Reticulocitos';
 reticulocitos.classList.add('botao');
 document.body.appendChild(reticulocitos);

 //Inicializa a função ao clicar no botao
 reticulocitos.addEventListener ('click', function(e){
   //cria o espaço de contagem
   document.createElement('div');

   //Painel
    const campoReti = document.createElement('div').value(0);
    campo.innerHTML=`${campoReti.value}`
    const valorReti = document.createElement('div').value(0);
    valor.innerHTML=`${valorReti.value}`
    
   //contagem

    


 });